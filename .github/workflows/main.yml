name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a placeholder check
        run: |
          echo "SyncEngine Workflow: main.yml is active."
          echo "Automated archiving process verified."
